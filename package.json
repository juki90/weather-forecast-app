{
  "name": "weather-forecast-app",
  "version": "1.0.0",
  "description": "",
  "private": true,
  "main": "index.js",
  "scripts": {
    "dev": "concurrently --kill-others \"nodemon server.js\" \"npm run scss-compile\"",
    "scss-compile": "node-sass --watch public/style.scss public/style.css",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.19.2",
    "express": "^4.17.1"
  },
  "devDependencies": {
    "concurrently": "^5.1.0",
    "dotenv": "^8.2.0",
    "node-sass": "^4.13.1",
    "nodemon": "^2.0.2"
  }
}
